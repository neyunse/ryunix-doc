import "@/styles/global.css";
import { themeInitScript } from "@/i18n/themeCookie.js";

const RootLayout = ({ children }) => {
  return (
    <>
      <script dangerouslySetInnerHTML={{ __html: themeInitScript }} />
      {children}
    </>
  );
};

export default RootLayout;
